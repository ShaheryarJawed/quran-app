<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Duas Collection - DeenSphere</title>

    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Amiri:ital,wght@0,400;0,700;1,400&family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;500;600;700;800&family=Playfair+Display:ital,wght@0,600;1,600&display=swap"
        rel="stylesheet">

    <!-- Icons -->
    <link href="https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css" rel="stylesheet">
    <link rel="stylesheet" href="style.css?v=3.0">
    <script src="shared_auth.js"></script>
    <script src="duas_data.js"></script>

    <style>
        /* Local Page Styles */
        .dua-hero {
            text-align: center;
            padding: 4rem 1rem;
            position: relative;
        }

        .dua-title-main {
            font-family: var(--font-display);
            font-size: 3.5rem;
            background: linear-gradient(to right, #fbbf24, #fff);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1rem;
        }

        .dua-subtitle {
            font-size: 1.1rem;
            color: #94a3b8;
            max-width: 600px;
            margin: 0 auto;
            line-height: 1.6;
        }

        .category-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
            padding-bottom: 5rem;
        }

        .dua-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 20px;
            padding: 2rem;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            height: 100%;
        }

        .dua-card:hover {
            transform: translateY(-5px);
            background: rgba(255, 255, 255, 0.08);
            border-color: #fbbf24;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }

        .dua-card-icon {
            width: 60px;
            height: 60px;
            background: rgba(4, 120, 87, 0.3);
            border-radius: 15px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.8rem;
            color: #fbbf24;
            margin-bottom: 1.5rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .dua-card h3 {
            font-family: var(--font-display);
            font-size: 1.5rem;
            color: #f1f5f9;
            margin: 0 0 0.5rem 0;
        }

        .dua-card p {
            font-size: 0.95rem;
            color: #94a3b8;
            line-height: 1.5;
            flex: 1;
        }

        .dua-card-arrow {
            margin-top: 1.5rem;
            color: #fbbf24;
            font-size: 1.2rem;
            opacity: 0;
            transform: translateX(-10px);
            transition: all 0.3s;
        }

        .dua-card:hover .dua-card-arrow {
            opacity: 1;
            transform: translateX(0);
        }

        /* --- MODAL --- */
        .dua-modal-overlay {
            position: fixed;
            inset: 0;
            background: rgba(0, 0, 0, 0.8);
            backdrop-filter: blur(8px);
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: all 0.3s;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .dua-modal-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .dua-modal-content {
            background: #0f172a;
            width: 100%;
            max-width: 800px;
            height: 85vh;
            border-radius: 24px;
            border: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            flex-direction: column;
            position: relative;
            transform: scale(0.95);
            transition: all 0.3s;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
        }

        .dua-modal-overlay.active .dua-modal-content {
            transform: scale(1);
        }

        .modal-header {
            padding: 1.5rem 2rem;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(255, 255, 255, 0.02);
            border-radius: 24px 24px 0 0;
        }

        .modal-title {
            font-family: var(--font-display);
            font-size: 1.8rem;
            color: #fbbf24;
        }

        .close-modal-btn {
            background: transparent;
            border: none;
            color: #94a3b8;
            font-size: 1.5rem;
            cursor: pointer;
            transition: 0.2s;
        }

        .close-modal-btn:hover {
            color: white;
        }

        .modal-body {
            flex: 1;
            overflow-y: auto;
            padding: 2rem;
        }

        .single-dua-item {
            background: rgba(255, 255, 255, 0.03);
            border-radius: 16px;
            padding: 2rem;
            margin-bottom: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.05);
        }

        .sd-arabic {
            font-family: var(--font-arabic);
            font-size: 2rem;
            color: white;
            text-align: right;
            line-height: 2.2;
            margin-bottom: 1.5rem;
            direction: rtl;
        }

        .sd-trans-ur {
            font-family: 'Noto Nastaliq Urdu', serif;
            color: #fbbf24;
            font-size: 1.1rem;
            text-align: right;
            margin-bottom: 1rem;
            line-height: 1.8;
            direction: rtl;
        }

        .sd-trans-en {
            color: #cbd5e1;
            font-size: 1rem;
            line-height: 1.6;
            margin-bottom: 1rem;
            font-style: italic;
        }

        .sd-ref {
            display: inline-block;
            background: rgba(4, 120, 87, 0.2);
            color: #6ee7b7;
            padding: 0.3rem 0.8rem;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        /* Scrollbar */
        .modal-body::-webkit-scrollbar {
            width: 8px;
        }

        .modal-body::-webkit-scrollbar-track {
            background: rgba(0, 0, 0, 0.2);
        }

        .modal-body::-webkit-scrollbar-thumb {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 4px;
        }

        .modal-body::-webkit-scrollbar-thumb:hover {
            background: rgba(255, 255, 255, 0.2);
        }
    </style>
</head>

<body class="portal-body">

    <!-- 1. PORTAL NAVIGATION -->
    <nav class="portal-nav">
        <div class="portal-container nav-flex">
            <div class="nav-brand-portal">
                <i class='bx bxs-moon pulse-icon'></i>
                <span>DeenSphere <span style="font-weight:300; opacity:0.7;">Pro</span></span>
            </div>
            <div class="nav-links-portal">
                <a href="index.html" class="p-link">Dashboard</a>
                <a href="quran.html" class="p-link">Quran</a>
                <a href="hadith.html" class="p-link">Hadith</a>
                <a href="seerah.html" class="p-link">Seerah</a>
                <a href="tasbih.html" class="p-link">Tasbih</a>
                <a href="duas.html" class="p-link active">Duas</a>
            </div>
            <!-- Profile/Actions -->
            <div id="header-actions-container" class="nav-actions-portal">
                <div id="auth-container">
                    <!-- Default: Guest State -->
                    <a href="auth.html" id="btn-signin" class="btn-portal-secondary"
                        style="color:white; border-color:rgba(255,255,255,0.3); padding:0.4rem 1.2rem; font-size:0.9rem; text-decoration:none; display:inline-block;">
                        Sign In
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <!-- 2. MAIN CONTENT -->
    <main class="portal-container">

        <div class="dua-hero">
            <h1 class="dua-title-main">Essential Supplications</h1>
            <p class="dua-subtitle">Authentic Duas from the Quran and Sunnah for every aspect of your life. Categorized
                for ease of access and reflection.</p>
        </div>

        <div class="category-grid" id="dua-grid">
            <!-- Cards Injected JS -->
        </div>

    </main>

    <!-- 3. DETAIL MODAL -->
    <div class="dua-modal-overlay" id="dua-modal">
        <div class="dua-modal-content">
            <div class="modal-header">
                <h3 class="modal-title" id="m-title">Topic Name</h3>
                <button class="close-modal-btn" onclick="closeModal()"><i class='bx bx-x'></i></button>
            </div>
            <div class="modal-body" id="m-body">
                <!-- Data Content -->
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            renderCategories();
        });

        const grid = document.getElementById('dua-grid');
        const modal = document.getElementById('dua-modal');
        const mTitle = document.getElementById('m-title');
        const mBody = document.getElementById('m-body');

        function renderCategories() {
            if (!window.DUA_DATA) {
                grid.innerHTML = '<p style="text-align:center; color:red;">Error: Dua Data not found.</p>';
                return;
            }

            let html = '';
            for (const [key, category] of Object.entries(window.DUA_DATA)) {
                html += `
                    <div class="dua-card" onclick="openCategory('${key}')">
                        <div class="dua-card-icon">
                            <i class='bx ${category.icon}'></i>
                        </div>
                        <h3>${category.title}</h3>
                        <p>${category.description}</p>
                        <i class='bx bx-right-arrow-alt dua-card-arrow'></i>
                    </div>
                `;
            }
            grid.innerHTML = html;
        }

        window.openCategory = function (key) {
            const cat = window.DUA_DATA[key];
            if (!cat) return;

            mTitle.textContent = cat.title;

            // Build Duas List
            let listHtml = '';
            cat.duas.forEach((dua, index) => {
                const duaId = `${key}:${index}`;
                const isLiked = isDuaLiked(duaId);

                listHtml += `
                    <div class="single-dua-item" style="position:relative;">
                        <button onclick="toggleDuaLike('${key}', ${index})" class="dua-like-btn" style="position:absolute; top:1.5rem; left:1.5rem; background:transparent; border:none; cursor:pointer;">
                             <i id="like-icon-${duaId}" class='bx ${isLiked ? 'bxs-heart' : 'bx-heart'}' style="font-size:1.5rem; color:${isLiked ? '#ef4444' : '#64748b'}; transition:all 0.2s;"></i>
                        </button>

                        <div class="sd-arabic">${dua.arabic}</div>
                        <div class="sd-trans-ur">${dua.translation_ur}</div>
                        <div class="sd-trans-en">${dua.translation_en}</div>
                        <div class="sd-ref">${dua.reference}</div>
                    </div>
                `;
            });
            mBody.innerHTML = listHtml;

            modal.classList.add('active');
        };

        // --- DUA LIKE LOGIC ---
        function getFavDuas() {
            return JSON.parse(localStorage.getItem('user_bookmarks_duas') || '[]');
        }

        window.isDuaLiked = function (id) {
            return getFavDuas().some(d => d.id === id);
        };

        window.toggleDuaLike = function (key, index) {
            const duaId = `${key}:${index}`;
            const cat = window.DUA_DATA[key];
            const dua = cat.duas[index];

            let favs = getFavDuas();
            const existingIndex = favs.findIndex(d => d.id === duaId);
            const icon = document.getElementById(`like-icon-${duaId}`);

            if (existingIndex > -1) {
                // Remove
                favs.splice(existingIndex, 1);
                if (icon) {
                    icon.classList.remove('bxs-heart');
                    icon.classList.add('bx-heart');
                    icon.style.color = '#64748b';
                }
            } else {
                // Add
                favs.push({
                    id: duaId,
                    category: cat.title,
                    arabic: dua.arabic.substring(0, 50) + "...",
                    translation: dua.translation_en.substring(0, 50) + "..."
                });
                if (icon) {
                    icon.classList.remove('bx-heart');
                    icon.classList.add('bxs-heart');
                    icon.style.color = '#ef4444';
                }
            }
            localStorage.setItem('user_bookmarks_duas', JSON.stringify(favs));

            // Also update main "bookmarks" count in dashboard (Vault) if we want unified count?
            // User requested "My Vault" showing "Bookmarks". I mapped that to 'user_bookmarks'.
            // I should probably map Duas to 'user_bookmarks' OR update vault to read 'user_bookmarks_duas' + 'user_bookmarks'.
            // For simplicity, I'll store them in a separate key 'user_bookmarks_duas' but I need to ensure Vault counts them.
            // Actually, I'll update Vault logic in script.js to sum them up or show separate? 
            // In script.js I used `user_bookmarks`. I will stick to `user_bookmarks_duas` here to avoid collision with generic bookmarks.
        };

        window.closeModal = function () {
            modal.classList.remove('active');
        };

        // Close on outside click
        modal.addEventListener('click', (e) => {
            if (e.target === modal) closeModal();
        });

        // ESC key
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape' && modal.classList.contains('active')) closeModal();
        });

    </script>
</body>

</html>